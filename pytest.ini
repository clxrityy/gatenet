[pytest]
pythonpath = src
filterwarnings =
    ignore:coroutine '.*' was never awaited:RuntimeWarning
    ignore:.*Event loop is closed:RuntimeWarning
markers =
    integration: mark a test as an integration test.
    timeout: set a timeout for a test (pytest-timeout plugin).